from setuptools import setup, find_packages

setup(
    name='F1T',
    version='0.1',
    packages=find_packages(),
    description="Make Project",
    author='Gentrit Gashi',
    install_requires=[
        
    ],
)